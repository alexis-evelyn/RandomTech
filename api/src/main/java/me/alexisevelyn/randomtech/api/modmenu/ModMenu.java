package me.alexisevelyn.randomtech.api.modmenu;

import io.github.prospector.modmenu.api.ModMenuApi;
import org.apiguardian.api.API;

/**
 * For adding mod menu config screen.
 *
 * Internal use only
 */
@API(status = API.Status.INTERNAL)
public class ModMenu implements ModMenuApi {
    // Intentionally Left Blank For Now

//    public ConfigScreenFactory<?> getModConfigScreenFactory() {
//        return parent -> {
//            //MainScreen config = AutoConfig.getConfigHolder(MainScreen.class).getConfig();
//
//            return AutoConfig.getConfigScreen(ConfigScreen.class, parent).get();
//        };
//    }
}